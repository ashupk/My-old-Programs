#include<stdio.h>
int dot_product(int num1[],int num2[],int size){
int i;
int dot;
    for(i=0;i<size;i++){
        dot=(num1[i])*(num2[i]);
        }
        return dot;
        }
    int main(){
        int size;
        int j,k;
        int num1[size],num2[size];
        scanf("%d",&size);
        for(j=0;j<size;j++){
            scanf("%d",&num1[j]);
            }
        for(k=0;k<size;k++){
            scanf("%d",&num2[k]);
            }
        printf("%d",};
        }
        return 0;
    }
